class Main {
  static void main(int i, int j) {
    while( i > 0 ){
      i := i - 10;
    }

    while (j > 0){
      j := j - 10;
    }

    int z := i + j;
    bool q := z <= 0;
    assert q;
  }
}